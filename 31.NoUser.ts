let user_names:string[] = ["admin", "ruba" ,"taha","saif","saba"];

if (user_names.length === 0){
    console.log("We need to find some users!");
}

//clearing the array 
user_names.length = 0;

//checking if the array is empty
if (user_names.length === 0) {
    console.log("We need to find some users!");
};